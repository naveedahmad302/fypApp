import { RuntimeConfig } from './env';

// DISABLED - No runtime overrides applied
// To enable, uncomment and set your values:
// export const RUNTIME_OVERRIDES: Partial<RuntimeConfig> = {
//   apiBaseUrl: 'http://localhost:8000',
// };

export const RUNTIME_OVERRIDES: Partial<RuntimeConfig> = {};

export default RUNTIME_OVERRIDES;
